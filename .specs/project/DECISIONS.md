# DECISIONS

## 2026-09-08 (later) — First real GitHub Actions run of gates.yml: 5 bugs found+fixed, GHCR live, branch protection blocked by plan
User asked to apply the two remaining pendências (branch protection + product-repo rollout) now that the earlier bugs were fixed. Before touching branch protection, pushed the Gate Hardening commits to `origin/main` for the first time ever (they'd only existed locally until now) — which meant `.github/workflows/gates.yml` ran for real on GitHub Actions for the first time in this initiative's history. It failed, repeatedly, each time on a genuinely new real bug — none of them guessed, all found via reading actual run logs (never trusting a green/red summary without checking):
1. `hashFiles()` used in a JOB-level `if:` — only legal at STEP level (confirmed via `actionlint`, which yamllint can't check). Fixed: moved to step-level guards on every affected step (also incidentally fixes a real branch-protection risk: a job-level `if:`-false job never runs, so a required check depending on it would hang forever).
2. `.harness-sandbox/docker/Dockerfile.sandbox` didn't exist in `agents-harness` — that path only exists in TARGET projects after `install.sh` runs; the template source repo itself never had it. Fixed with a symlink (`.harness-sandbox/docker -> ../docker`), not a physical copy — single source of truth, zero drift risk.
3. Every `docker run ... harness-sandbox:ci bash <script>` line actually executed `/bin/bash bash <script>` (ENTRYPOINT is already `/bin/bash`) — bash treats "bash" as the script name, searches $PATH, finds itself, fails with "cannot execute binary file". Invisible until now because `sandbox-run.sh` (local usage) never passes a command, just opens an interactive shell. Fixed by dropping the redundant leading `bash`/`bash -c`→`-c` everywhere. Reproduced and confirmed the exact fix with plain bash, no Docker needed.
4. `skills/...` didn't exist at `agents-harness`'s root — same root cause as #2 (`agents-harness` keeps its real skills under `claude/skills/`, never ran `install.sh` on itself). Fixed with a second symlink (`skills -> claude/skills`).
5. `ghcr.io/${{ github.repository_owner }}/...` produced `ghcr.io/rastaFul/...` — GHCR rejects non-lowercase repo names, and GitHub Actions' expression language has no case-folding function. Fixed with a real shell step (`tr '[:upper:]' '[:lower:]'`) feeding a step output used in the tag instead.

Result: full green run (`id 34292146125`, all 7 jobs including `publish-image`) confirmed via `gh api`, not the task notification. Multi-arch image genuinely pushed to GHCR — confirmed via the build-push-action log itself (manifest list digest `sha256:ed708d...`, two per-arch manifests + attestations, `pushing manifest ... done`), not just a green checkmark.

**Branch protection: blocked by a real GitHub plan limitation, not a bug.** Both the classic branch-protection API and the newer rulesets API return `403 "Upgrade to GitHub Pro or make this repository public to enable this feature."` for private repos on a free personal plan — tested live against `agents-harness` itself (the lowest-risk repo, per the runbook's own suggested order), not assumed. This blocks EVERY private repo in scope (agents-harness, artists-booking, microgrow, rastafinancas, vetcare, infra-platform) equally. Two real options exist (upgrade to Pro, or make a given repo public) — neither decided here, this is a business/visibility decision for the user, not a technical one for the agent to make. `docs/runbooks/branch-protection.md` updated with this finding and the confirmed real job-context names (from the actual green run) so it's ready to execute the moment either option is chosen.

**Product-repo rollout:** not started. Given the scale (5 repos, each potentially in a different state — existing CI, existing `package.json`, etc.) and that this is explicitly new scope beyond the original Gate Hardening SPEC.md, asked the user to either pick a starting repo or treat this as its own spec rather than silently running `install.sh` against all 5.

**INFRACOST_API_KEY:** not provisioned — genuinely requires the user to sign up (human/browser action), not something the agent can do unilaterally. Asked whether they already have a key or want it deferred.

## 2026-09-08 — QUESTIONS.pt-BR.md rollout (Task 7, all 20 items resolved/executed)
User answered every open question from the Gate Hardening initiative in Portuguese (`QUESTIONS.pt-BR.md`). Executed all of them in one pass. Summary (full rationale inline in each script/file's own comments):
- **Cost (Infracost):** threshold = \$0 baseline (local-only infra today), any nonzero monthly cost blocks. `run-cost-gate.sh` now fails on `monthlyCostUSD > 0` instead of reporting INFO. Follow-up noted (not done): switch to a true diff-based gate once a first paid resource is deliberately provisioned.
- **Lighthouse CI:** 90/100 minimum on all 4 categories (performance/accessibility/best-practices/SEO) — Google's own documented "green" band, the market-standard bar, not invented. `run-lighthouse.sh` now blocks below threshold; overridable via env vars per project.
- **Stryker (mutation testing):** explained to the user in chat (re-runs the test suite while injecting small code mutations — a test suite that still passes against a mutant is a weak-test signal coverage % can't catch). `thresholds.break` set to 50 (Stryker's own recommended starting point, below `low=60`) as a guardrail pending a real baseline measurement on an actual project.
- **jscpd (duplication):** 3% threshold — SonarQube's long-standing market-standard duplication bar, applied here for consistency.
- **Semgrep:** `WARNING` severity now blocks, not just `ERROR` (user chose to include it).
- **osv-scanner:** `unscored` findings (no normalized severity) now fail-closed, treated as HIGH.
- **Uniform security bar:** confirmed — same 0-critical/0-high bar across all 6 security tools including `trivy_config` (IaC misconfig), no differential treatment, for simplicity (user's explicit preference).
- **Container registry:** GHCR (ghcr.io) — NOT a new decision, this is `infra-platform`'s own ADR 006 (already the platform-wide registry choice: "build→push GHCR→SSH→docker compose up"). Added a `publish-image` job to `.github/workflows/gates.yml` that builds+pushes a multi-arch image to GHCR on merge to `main`, using the workflow's automatic `GITHUB_TOKEN` — no new secret/account needed.
- **INFRACOST_API_KEY / branch protection:** both are real external/admin actions the agent cannot or should not execute unilaterally (account creation needs human interaction; branch protection is a cross-repo admin action explicitly out of the original SPEC.md scope). Delivered as runbooks (`docs/runbooks/infracost-api-key.md`, `docs/runbooks/branch-protection.md`) per the user's own request ("quero o runbook"), not auto-applied to any repo.
- **CODEOWNERS:** created (`.github/CODEOWNERS`, `* @rastaFul`, confirmed live via `gh api user`), copied into target projects by `install.sh`. Enforcing it (branch protection's "require code owner review") is documented in the branch-protection runbook, not auto-enabled.
- **Tool version pinning + Renovate:** `kubeconform` (v0.8.0) and `kube-linter` (v0.8.3) — the last 2 tools still on `/latest/download/` — are now pinned like every other tool in `Dockerfile.sandbox`. Added `renovate.json` at repo root with a regex custom manager per `ARG *_VERSION`, PR-only (no automerge) — "sempre com revisão antes" per the user's answer. Dependabot's built-in docker ecosystem doesn't cover curl-installed ARG-pinned binaries, hence Renovate's custom regex manager instead.
- **ARM64 support:** added `ARG TARGETARCH` (auto-populated by BuildKit/buildx; a plain `docker build` still defaults to host arch, so this machine's local amd64 builds are unaffected) and per-tool arch mapping to every tool in `Dockerfile.sandbox` that was hardcoded to amd64 (14 tools total: gitleaks, osv-scanner, kube-bench, tfsec, kube-score, terraform, terraform-docs, polaris, pluto, opa, conftest, kubeconform, kube-linter, plus sonar-scanner). Every arm64 asset URL was verified live (`curl -o /dev/null -w '%{http_code}'` = 200) against each tool's actual pinned version before being added — not guessed. `sonar-scanner-cli`'s pin had to be bumped (5.0.1.3006 → 8.1.0.6389) because its old pin predates per-arch releases entirely (no arm64 asset exists under any filename for that version; Linux aarch64 builds only exist from SonarScanner CLI v6.1 onward). Added a `publish-image` CI job using `docker/setup-qemu-action` + `buildx` to actually build+push `linux/amd64,linux/arm64` — genuine multi-arch, not just Dockerfile plumbing that's never exercised. All 14 arch-mapped tools were re-verified functionally (real `--version`/`version` execution, not file-presence) after the rebuild, on this machine's amd64 host.
- **terraform-docs `--output-check` / Polaris JSON fields (items #15/#16):** verified for real against fresh fixtures (a minimal `.tf` module + a deliberately-bad K8s Pod manifest), not left as "assumed correct." `terraform-docs --output-check` confirmed to PASS when current and FAIL (exit 1) when stale, exactly as coded. Polaris's real JSON schema (v10.2.2) has **no** top-level `DangerResultCount`/`WarningResultCount` field at all (the original guess was wrong, as flagged) — real counts require walking `Results[].PodResult.Results{}` + `.ContainerResults[].Results{}` and counting `Success:false` entries by `Severity`. Fixed the parser in `run-infra-quality-final.sh` to do this for real.
- **2 additional real bugs found (not part of #15/#16, but only surfaced by actually executing this verification, same discipline as the rest of this initiative):**
  1. `MODULE_DIRS=$(find ... | xargs -n1 dirname | sort -u)` crashed the ENTIRE final infra-quality script (exit 123) on any repo with zero `.tf` files — an ordinary case, not an edge case — because `xargs` without `-r`/`--no-run-if-empty` runs the target command once even with empty stdin, and `dirname` with zero args errors. Fixed with `-r` (confirmed supported by both BusyBox and GNU xargs).
  2. Both Polaris and kube-linter write a status line to **stderr** even in `--format json` mode (confirmed live: `time="..." level=info msg="3 danger items found in audit"` and `Error: found 5 lint errors`). The script merged stderr into the captured output via `2>&1` before `json.load`, silently corrupting the JSON — the except-block swallowed the parse error and always reported `danger`/`warning`/`reports` as 0 regardless of real findings (PASS/FAIL itself stayed correct, since that's driven by the tool's exit code, not this parse). Fixed by keeping stdout/stderr separate (`2>/dev/null`) in both places. Proactively checked `tflint`/`kubeconform`/`pluto` (same script family, same `2>&1`-before-json.load pattern) for the identical bug — all three have empty stderr in practice, confirmed via the same fixture-based real execution, no fix needed there.
- **Dev-quality bundle auto-install (item #17):** `install.sh` now auto-runs `npm install -D <bundle>` + `husky/setup-husky.sh` whenever a target project has a `package.json`, same policy as the pre-existing `axe-playwright` auto-install — resolves the inconsistency the user flagged (previously document-only).
- **Sync policy (item #18):** confirmed as an explicit, standing instruction — after every validated change, sync `claude/` (agents + skills + steering + CLAUDE.md) to `~/.claude`, diff-first, never blind-overwrite (same discipline as prior sessions). This session's own closing step.
- **`harness-dev.md` gate parity (item #19a):** added the same "Architecture conformance / Duplication-complexity / Mutation testing / Security scanning / Perf-A11y" bullets to rule 3 that `harness-infra.md` already had for its own gate family — closes the "written but orphaned" doc gap.
- **`run-final.sh` wiring (item #19):** `code-gates/scripts/run-final.sh` now calls `run-architecture-gate.sh`, `run-quality-extra.sh`, and `run-mutation.sh` and merges their results in, instead of leaving them standalone/uncalled.

All of the above verified where verification was possible in this sandbox: `bash -n` on every changed script, JSON/YAML validity, `hadolint` (0 findings) on the changed `Dockerfile.sandbox`, a full `docker build` rebuild (REAL_EXIT=0, marker-checked not notification-trusted), and real functional execution of all 14 arch-mapped tools plus the terraform-docs/Polaris/kube-linter fixture tests described above.

## 2026-09-03 — real Terraform installed via HashiCorp binary, not apk
`terraform` was never a valid Alpine apk package (BUSL license, not apk-distributed) — this was already broken in the Dockerfile before this session, confirmed live via `apk search -x terraform` (empty) vs `apk search -x opentofu` (found, real package). Fixed by installing Terraform via HashiCorp's official binary release, same pattern as tfsec/kube-score. Kept `opentofu` via apk as-is. Version pinned to 1.16.1 — my first attempt (1.17.0) was wrong, scraped from HTML text on the releases page that wasn't an actual published version; corrected against the authoritative `releases.hashicorp.com/terraform/index.json` with real semver sort, and confirmed by actually downloading the file (HTTP 200, valid zip signature) before touching the Dockerfile again.

## 2026-09-03 — added `-f` (fail-on-HTTP-error) to every raw `curl -o`/`-Lo` download in Dockerfile.sandbox
The wrong-terraform-version bug above only surfaced as a build failure because `unzip` choked on garbage — the `curl -sSLo` that fetched it had no `-f`, so a 404 HTML/XML error page downloaded "successfully" and was silently treated as the real file. Audited the whole file for the same class of bug: 6 more downloads (terraform-docs, polaris, pluto, kubeconform, kube-linter, and the pre-existing sonar-scanner line) had the same gap. All fixed. This is a systemic fix, not just a terraform fix — prevents the same silent-corruption failure mode from recurring on any future version bump of any of these tools.

## 2026-09-03 — removed `npm install -g npm@latest` from Dockerfile.sandbox
Also pre-existing, different bug, same apk-add line: on `node:20-alpine`, "latest" npm (12.x) requires Node >=22, breaks immediately (EBADENGINE). Node 20 already bundles npm 10.8.2, sufficient for every later `npm install -g <tool>` step in the file. Removed rather than pinned to a fake-compatible-sounding version — nothing downstream needs npm >10.

## 2026-09-03 — replaced tfsec's and kube-score's official install scripts with direct binary downloads
tfsec's script is fragile in two independent ways on this image (rate-limited GitHub API call to resolve "latest"; `sha256sum --quiet`, a GNU-only flag busybox doesn't support). kube-score's install.sh no longer exists upstream at all (404 on both `master` and `main`). Rather than patch upstream scripts, switched both to the same direct-pinned-binary pattern already used for gitleaks/osv-scanner/kube-bench elsewhere in the file.

## 2026-09-03 — infracost install switched from `infracost/infracost` to `infracost/cli`
The old repo's own install script says it only supports versions <1.0.0 and points to `infracost/cli` for anything current (now at v2.16.2). Using the correct, actively-maintained repo and its installer.

## 2026-09-03 — added `gcompat` to the sandbox base image
`infracost`'s binary (and, separately, sonar-scanner's bundled JVM — see below) is glibc-linked; Alpine is musl-based. `gcompat` (Alpine's glibc-compat shim) fixed infracost. Confirmed via `readelf -l` (missing `/lib64/ld-linux-x86-64.so.2` interpreter) before adding it, not guessed.

## 2026-09-03 — sonar-scanner's JVM failure left unfixed (scope decision)
`gcompat` isn't sufficient for a full JVM (`libjimage.so` still fails to load) — real glibc or a different scanner distribution would be needed, a materially bigger change than anything else in this pass. Left as a documented, known gap (QUESTIONS.md #20) rather than either silently ignoring it or open-endedly expanding scope to fix a pre-existing tool that isn't one of the 20 new gates this initiative was scoped to deliver.

## 2026-09-03 — CI builds Dockerfile.sandbox fresh instead of pulling a published registry image
Avoids needing a registry-publishing decision (ghcr.io org/auth) to satisfy the "local == CI" hard constraint — both sides build from the same in-repo Dockerfile. Registry publishing left as a future speed optimization only, not a blocker (see QUESTIONS.md item 9).


## 2026-09-02 — claude-auto-retry lives in agents-harness, not infra-platform
Rationale: it's meta-tooling for how Claude Code agents run (session/rate-limit management), not cloud infra. agents-harness already owns autonomous-mode rules (rule 9) and has the install.sh pattern (mirrors `install_snip`). Ansible project was considered and rejected — it would configure a remote server to manage a local CLI session, wrong layer.

## 2026-09-02 — retryMessage forces STATE.md re-read instead of blind "continue"
Rationale: claude-auto-retry's tmux send-keys injection cannot detect special confirmation prompts (upstream limitation) — treating its resume as guaranteed context-perfect is unsafe. Pairing it with the harness's own checkpoint protocol (every 3 steps/15min) makes the imperfect injection safe: worst case the agent re-grounds like a fresh session.

## 2026-09-02 — mid-task checkpoint frequency generalized to every task
Rationale: rule 9's 3-steps/15min checkpoint only applied to autonomous mode — a normal task interrupted mid-flight (rate limit, crash) had no intermediate record, only IN_PROGRESS at start and DONE/FAILED at end. Reused the existing cadence constant instead of introducing a second one. Format/content unchanged (user explicit constraint) — only write frequency increased. Applied to harness-infra.md, harness-dev.md (rules 2 and 5) and task-executor.md (new rule 5b), synced to ~/.claude.

## 2026-09-02 — --remote-control mandatory for all autonomous launches and hard relaunches
Rationale: user follows autonomous runs from mobile. Native Claude Code `--remote-control` flag covers this with no inbound ports / no infra change. claude-auto-retry resumes the *same* process (remote-control persists automatically), but a hard relaunch after a crash creates a genuinely new session — that must explicitly re-add the flag, since crash time is exactly when nobody's at the terminal.

## 2026-09-09 — Product-repo rollout complete, 11 real bugs found via live CI (not simulated)
User asked to create specs and execute the rollout to all 5 remaining repos (artists-booking, microgrow, rastafinancas, vetcare, infra-platform). Wrote a scoped `.specs/features/harness-gates-rollout/spec.md` per repo before touching anything, confirmed no conflicting pre-existing husky/dev-quality config in any of them, ran `install.sh` against each, verified files/syntax before committing, and staged ONLY the harness-installation paths in every repo (checked `git status` first every time — several repos had real unrelated WIP in progress, e.g. microgrow's `webapp/src/...` changes, infra-platform's own pending `.specs`/docs edits — none of it touched).

Rolling out to real, previously-untouched codebases immediately surfaced 11 real bugs that no amount of local syntax checking (bash -n, yamllint, actionlint, docker build) could have caught, because they only manifest against real repo content/CI environments: tracked Windows Zone.Identifier junk files polluting every install; `lint-staged@17+`'s git-version floor blocking the very first commit on this machine; a `grep -c || echo 0` duplication bug that crashed the gate chain on the NORMAL case of zero findings (found 3 separate times, in `run-gates.sh`, `run-policy-gate.sh`, and `run-mutation.sh`); a missing top-level `permissions:` in `gates.yml` (real checkov finding); and a hardcoded GHCR image name that denied every repo except agents-harness itself from publishing. All fixed centrally, re-verified via real re-execution (local repro where possible — several of these were reproduced and confirmed WITHOUT Docker, directly with host bash, which was much faster than round-tripping through GitHub Actions each time), and propagated to every affected repo + `~/.claude`.

**Decision: production Terraform findings in infra-platform (CKV_OCI_4, CKV_OCI_5) are left open, not auto-fixed.** These are real, previously-unknown gaps in the live OCI compute instance module — but that's a running production VM, and changing boot-volume encryption or metadata service settings may require replacing the instance (real blast radius, not a config tweak). Per rule 9 escalation criteria ("action targets production environment", "blast radius HIGH/irreversible"), this needs its own spec, `terraform plan` review, and explicit approval — not something to silently patch as a side effect of a gate-rollout task, even though it's technically "just" flipping two Terraform arguments. Recorded in infra-platform's own rollout spec outcome section, escalated to the user rather than guessed at.

Final state, verified via `gh api` on the actual completed runs (never trusted the background-task notification's own status, which was frequently `killed` for unrelated system memory pressure during the wait, not run failure): **4/5 rolled-out repos are fully green end to end** (all 7 gates.yml jobs passing, including the real multi-arch GHCR publish). **1/5 (infra-platform) is red for a legitimate, real reason**, not a bug.

## 2026-09-09 (later) — Autonomous-spirit loop closed all repos to zero red gates
User authorized fixing the 2 production Terraform findings ("pode mexer sim... quero seguir com o ambiente com a melhor qualidade possível") and asked to enter autonomous/loop mode until zero vulnerabilities or blocked gates remain. Same caveat as every prior "autonomous" request: cannot self-relaunch as a separate `--remote-control` process from inside a running session — applied rule 9's spirit instead (closed scope, checkpoints, 3-attempt circuit breaker per finding), documented explicitly in `.specs/project/SPEC.md` Task 10 rather than silently claiming full compliance.

Fixed the 2 real checkov findings (CKV_OCI_4 boot-volume in-transit encryption, CKV_OCI_5 legacy metadata endpoint) on `infra-platform`'s production OCI compute instance module — verified via official provider docs first (both fields Optional/Updatable/ForceNew:No, so `apply` updates in place, doesn't replace the instance) and via real `terraform validate`/`tfsec`/`checkov` runs (0 findings after). NOT applied to the live instance — no OCI/Terraform Cloud credentials in this session; the fix is real and verified, applying needs an environment with real credentials.

Applying that fix and the earlier `reusable-ci.yml` permissions fix (the explicit ask) surfaced infra-platform's `infra-gates` STILL failing — for two more real reasons, not yet visible until code that actually exercised them ran: `terraform validate`/`plan` assumed .tf files live at repo root (false for this repo's real `terraform/environments/oci-free/` layout — silently validated an empty directory, a false pass), and the OPA policy-gate step had no fallback for the very common "no cloud credentials in CI" case that every environment in this initiative hits. Both fixed, verified locally without needing Docker or real cloud credentials (terraform v1.9.8 installed on this machine sufficed), propagated to all repos.

**Closing result, verified via `gh api` on actually-completed runs (not trusted from any notification, and re-verified fresh after a mid-task rate-limit pause+resume per rule 9): all 6 repos in this initiative's footprint (agents-harness, artists-booking, microgrow, rastafinancas, vetcare, infra-platform) are fully green — 42/42 jobs (build-sandbox, dev-gates, security-gates, infra-gates, perf-a11y-gates, policy-gate-status, publish-image, ×6 repos) pass.** Zero vulnerabilities, zero blocked gates remaining within this task's closed scope. This closes out the entire Gate Hardening initiative's rollout arc — from zero real CI runs ever having succeeded (confirmed at the very start of this multi-day initiative) to full green across the whole footprint, entirely via real bugs found and fixed through actual execution, never guessed.
